apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: products
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      nodePort: 30081
